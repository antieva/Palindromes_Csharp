using System.Collections.Generic;
using System;

namespace PalindromesApp
{
  public class Palindrome
  {
    //return type string just for testing perposes, it should be void.
      public static string IsPalindrome(string myString)
      {
          string newString = myString.Trim();
          return newString;
      }
  }
}
